import { IndexPostPipePipe } from './index-post-pipe.pipe';

describe('IndexPostPipePipe', () => {
  it('create an instance', () => {
    const pipe = new IndexPostPipePipe();
    expect(pipe).toBeTruthy();
  });
});
