import { IndexPostPipe } from './index-post.pipe';

describe('IndexPostPipe', () => {
  it('create an instance', () => {
    const pipe = new IndexPostPipe();
    expect(pipe).toBeTruthy();
  });
});
